import React from 'react';

const Background: React.FC = () => {
  return (
    <>
      {/* <div className="particle-field" aria-hidden="true"></div>
      <div className="code-glyphs" aria-hidden="true">
        <span>{`{}`}</span><span>SELECT</span><span>JOIN</span><span>lambda</span><span>def</span><span>%%</span><span>GROUP BY</span><span>//</span>
        <span>async</span><span>await</span><span>map()</span><span>reduce()</span><span>forEach</span><span>try{`{}`}</span><span>ON CONFLICT</span><span>coalesce()</span>
      </div> */}
    </>
  );
};

export default Background;
